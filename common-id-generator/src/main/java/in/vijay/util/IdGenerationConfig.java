package in.vijay.util;

/*import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration*/
public class IdGenerationConfig {

/*    @Bean
    @Qualifier("categoryIdStrategy")
    public IdGenerationStrategy<String> categoryIdStrategy(StringIdGenerationStrategy stringIdStrategy) {
        return stringIdStrategy;
    }
    @Bean
    @Qualifier("productIdStrategy")
    public IdGenerationStrategy<String> productIdStrategy(StringIdGenerationStrategy stringIdStrategy) {
        return stringIdStrategy;
    }*/

    // Add more strategy beans for other entity types as needed
}
